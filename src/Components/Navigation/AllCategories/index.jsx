import Categories from "../Categories";

const AllCategories = () => {
  return (
    <div className="col-sm-3">
      <Categories
      // goTo={CATEGORIA_SUTIA["goTo"]}
      // iconName={CATEGORIA_SUTIA["icon"]}
      // nameMenu={CATEGORIA_SUTIA["name"]}
      // itemsMenu={CATEGORIA_SUTIA["categories"]}
      />
    </div>
  );
};

export default AllCategories;
